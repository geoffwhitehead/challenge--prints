import { Box, Typography } from "@mui/material";
import React from "react";

export const Home: React.FC = () => {
  return (
    <Box marginTop={2}>
      <Typography>Hi, use the navigation links to find content.</Typography>
    </Box>
  );
};
